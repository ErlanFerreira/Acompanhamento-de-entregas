# Dependências extras usadas SÓ pelo job de sincronização (GitHub Actions),
# que faz automação de navegador -- não usadas pelo app web na Vercel
# (playwright/openpyxl são pesados demais para o limite de tamanho de
# função serverless da Vercel, e o app web nem chama esse código).
-r requirements.txt
openpyxl==3.1.5
playwright==1.62.0
