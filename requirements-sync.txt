# Dependências extras usadas SÓ pelos jobs de automação (GitHub Actions) --
# não usadas pelo app web na Vercel (o navegador Chromium do Playwright é
# pesado demais para o limite de tamanho de função serverless da Vercel, e
# o app web nem chama esse código -- só dispara o workflow).
-r requirements.txt
playwright==1.62.0
